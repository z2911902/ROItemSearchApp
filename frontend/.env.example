# Vite dev proxy target. This value is only used by vite.config.ts.
DEV_API_TARGET=http://127.0.0.1:8000

# Optional browser-visible API base.
# Keep empty during normal local development so requests use /api through Vite proxy.
VITE_API_BASE_URL=
